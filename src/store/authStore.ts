import {create} from 'zustand';


export const useAuthStore = create((set)=>({
    // token:
    // accessTokenExpiry:
    // user:
    // isAuthenticated:

    login:()=>{},
    logout:()=>{},
    updateUser:()=>{}
}))